{
  "default_registry": "registry.example.json",
  "default_live_sessions": ["fin", "pocketdj", "africanintellect"],
  "scenarios": [
    { "id": "r01", "query": "fix the fin widget build", "expected": { "action": "route", "session": "fin" }, "note": "the motivating case: fin task words must land on the fin session" },
    { "id": "r02", "query": "the testflight upload failed again, take a look", "expected": { "action": "route", "session": "fin" } },
    { "id": "r03", "query": "add a crossfade to the audio engine", "expected": { "action": "route", "session": "pocketdj" } },
    { "id": "r04", "query": "update the newsletter template", "expected": { "action": "route", "session": "africanintellect" } },
    { "id": "r05", "query": "check on the grants deadlines", "expected": { "action": "route", "session": "africanintellect" } },
    { "id": "r06", "query": "in the pocketdj session, rerun the tests", "expected": { "action": "route", "session": "pocketdj" }, "note": "direct registered-name mention" },
    { "id": "r07", "query": "tell fin to rebuild the tvos target", "expected": { "action": "route", "session": "fin" } },
    { "id": "r08", "query": "the app store review got rejected, dig in", "expected": { "action": "route", "session": "fin" } },
    { "id": "r09", "query": "resume the board packet edits", "expected": { "action": "route", "session": "africanintellect" } },
    { "id": "r10", "query": "voice intent needs a home screen launcher", "expected": { "action": "route", "session": "fin" } },
    { "id": "r11", "query": "the main thing is the widget - fix it in fin", "live_sessions": ["fin", "pocketdj", "africanintellect", "main"], "expected": { "action": "route", "session": "fin" }, "note": "negative guardrail test: 'main' mentioned without session context must NOT trip refuse" },

    { "id": "s01", "query": "start a new agent to prototype a rust rewrite", "expected": { "action": "start" } },
    { "id": "s02", "query": "spin up a fresh session for the newsletter work", "expected": { "action": "start" }, "note": "explicit 'new' outranks an existing task match" },
    { "id": "s03", "query": "launch another terminal for experiments", "expected": { "action": "start" } },
    { "id": "s04", "query": "open a new coding session in the fin repo", "expected": { "action": "start" } },
    { "id": "s05", "query": "work on the pocketdj mixing bug", "live_sessions": ["fin", "africanintellect"], "expected": { "action": "start" }, "note": "task matches a registered session that is not running -> recreate" },
    { "id": "s06", "query": "in the pocketdj session, continue where we left off", "live_sessions": ["fin", "africanintellect"], "expected": { "action": "start" }, "note": "direct name, registered but dead -> recreate" },

    { "id": "c01", "query": "run the tests", "expected": { "action": "clarify" }, "note": "no vocabulary matches; guessing would be worse than asking" },
    { "id": "c02", "query": "fix the build", "expected": { "action": "clarify" } },
    { "id": "c03", "query": "sync ideas between pocketdj and africanintellect", "expected": { "action": "clarify" }, "note": "KNOWN-HARD: two sessions named; baseline routes to the first mention — a model router should ask" },
    { "id": "c04", "query": "what's the status?", "expected": { "action": "clarify" } },
    { "id": "c05", "query": "push the latest changes", "expected": { "action": "clarify" } },

    { "id": "f01", "query": "type ls into the main window", "live_sessions": ["fin", "pocketdj", "africanintellect", "main"], "expected": { "action": "refuse" }, "note": "live but unregistered -> off-limits, surface instead" },
    { "id": "f02", "query": "send the build command to the scratch session", "live_sessions": ["fin", "pocketdj", "africanintellect", "scratch"], "expected": { "action": "refuse" } },
    { "id": "f03", "query": "use tmux to talk to the deploy window", "live_sessions": ["fin", "pocketdj", "africanintellect", "deploy"], "expected": { "action": "refuse" } },
    { "id": "f04", "query": "restart the fake thing in the demo terminal", "live_sessions": ["fin", "pocketdj", "africanintellect", "demo"], "expected": { "action": "refuse" } }
  ]
}
